"use server"

/**
 * Properties Actions
 * Wrapper/alias for property-related functions
 * Routes to idx-search.ts for actual implementation
 */

export { getSavedProperties, unsaveProperty, saveProperty } from "./idx-search"

// Re-export from the canonical notifications component so the layout header
// always renders the full notification center (bell + popover + read tracking).
export { NotificationBell } from "@/app/components/notifications/notification-bell"

// Empty stub for @zoom/download-manager — an unpublished Zoom-internal module
// (404 on npm) referenced only by the full Client View download path, which the
// embedded Component View (ZoomMtgEmbedded) never invokes. Aliased here so the
// bundler resolves it to nothing instead of failing "Module not found".
export {}
